#!/bin/bash


# This script configures Git whenever you start working on multiple machines
# Run it anytime u decide to work with Git on a new machine.
git config --global user.name "Luke Horowitz"
git config --global user.email "lhorowit@calpoly.edu"
git config --global color.ui "auto"
git config --global core.editor "nano"
 
